<h1><%= @cloud.title %></h1>

<p><%= @cloud.body %></p>


<ul>
  <li><%= link_to "Edit", edit_cloud_path(@cloud) %></li>
  <li><%= link_to "Destroy", cloud_path(@cloud), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
